## Next.js TPs réalisés

* TP1 — App Router, routing par dossiers, layout.tsx, Server Components, fetch sans useEffect, Client Components ('use client')
* TP2 — Server Actions ('use server'), API Routes (GET/POST/PUT/DELETE), Auth cookies HttpOnly, Middleware, revalidatePath
