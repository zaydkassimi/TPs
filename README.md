# TPs réalisés

## React (branche main)
* TP1 — Init Vite, composants, props/state, json-server, useEffect + fetch
* TP2 — useReducer, Context, formulaire login, protected layout, useLayoutEffect
* TP3 — React Router v6, Axios, routes protégées, CRUD complet (GET/POST/PUT/DELETE)
* TP4 — MUI vs Bootstrap, composants UI, architecture BDD
* TP5 — Redux Toolkit, JWT simulé, intercepteur Axios, Custom Hooks, React.memo

## Next.js (branche nextjs)
* TP1 — App Router, routing par dossiers, layout.tsx, Server Components, fetch sans useEffect, Client Components
* TP2 — Server Actions ('use server'), API Routes (GET/POST/PUT/DELETE), Auth cookies HttpOnly, Middleware, revalidatePath
